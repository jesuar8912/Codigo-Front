import { CambiarTextoDirective } from './cambiar-texto.directive';

describe('CambiarTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiarTextoDirective();
    expect(directive).toBeTruthy();
  });
});
