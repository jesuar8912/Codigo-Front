import { AlcuadradoPipe } from './alcuadrado.pipe';

describe('AlcuadradoPipe', () => {
  it('create an instance', () => {
    const pipe = new AlcuadradoPipe();
    expect(pipe).toBeTruthy();
  });
});
