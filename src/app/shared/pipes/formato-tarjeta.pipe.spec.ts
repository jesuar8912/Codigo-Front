import { FormatoTarjetaPipe } from './formato-tarjeta.pipe';

describe('FormatoTarjetaPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatoTarjetaPipe();
    expect(pipe).toBeTruthy();
  });
});
